export * from './defaults';

//components is split up into 3 categories. defaults, essential and specific
//defaults are the most basic components that replace default jsx components
//essentials are components that are used app-wide but are not defaults
//specifics are components that are used in specific places in the app
